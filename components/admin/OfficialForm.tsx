import Link from "next/link";
import ImageUploadField from "@/components/admin/ImageUploadField";
import { saveOfficial } from "@/app/admin/(dashboard)/actions";
import type { Official } from "@/lib/types";
export default function OfficialForm({item}:{item?:Official|null}){return <form className="admin-panel form-grid" action={saveOfficial}>{item?<input type="hidden" name="id" value={item.id}/>:null}<div className="field"><label>Nama</label><input name="name" required defaultValue={item?.name||""}/></div><div className="field"><label>Jabatan</label><input name="position" required defaultValue={item?.position||""}/></div><div className="field span-2"><label>Biografi Singkat</label><textarea name="biography" defaultValue={item?.biography||""}/></div><ImageUploadField name="photo_url" defaultValue={item?.photo_url} folder="officials" label="Foto Perangkat"/><div className="field"><label>Urutan</label><input type="number" name="display_order" defaultValue={item?.display_order||0}/></div><label className="checkbox-field"><input type="checkbox" name="is_published" defaultChecked={item?.is_published}/> Publikasikan</label><div className="form-actions"><button className="button primary">Simpan</button><Link className="button danger" href="/admin/perangkat">Batal</Link></div></form>}
