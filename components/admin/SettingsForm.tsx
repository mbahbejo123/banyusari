import ImageUploadField from "@/components/admin/ImageUploadField";
import { saveSettings } from "@/app/admin/(dashboard)/actions";
import type { SiteSettings } from "@/lib/types";
export default function SettingsForm({ item }: { item: SiteSettings | null }) { return <form className="admin-panel form-grid" action={saveSettings}><div className="field"><label>Nama Desa</label><input name="village_name" defaultValue={item?.village_name || ""} /></div><div className="field"><label>Nama Kepala Desa</label><input name="village_head_name" defaultValue={item?.village_head_name || ""} /></div><div className="field"><label>Kecamatan</label><input name="district" defaultValue={item?.district || ""} /></div><div className="field"><label>Kabupaten</label><input name="regency" defaultValue={item?.regency || ""} /></div><div className="field"><label>Provinsi</label><input name="province" defaultValue={item?.province || ""} /></div><div className="field"><label>Kode Pos</label><input name="postal_code" defaultValue={item?.postal_code || ""} /></div><div className="field span-2"><label>Alamat Kantor</label><textarea name="address" defaultValue={item?.address || ""} /></div><div className="field"><label>Telepon</label><input name="phone" defaultValue={item?.phone || ""} /></div><div className="field"><label>Surel</label><input type="email" name="email" defaultValue={item?.email || ""} /></div><div className="field span-2"><label>Sambutan Beranda</label><textarea name="welcome_message" defaultValue={item?.welcome_message || ""} /></div><div className="field span-2"><label>Sejarah Desa</label><textarea name="history" defaultValue={item?.history || ""} /></div><div className="field span-2"><label>Visi</label><textarea name="vision" defaultValue={item?.vision || ""} /></div><div className="field span-2"><label>Misi</label><textarea name="mission" defaultValue={item?.mission || ""} /></div><ImageUploadField name="logo_url" defaultValue={item?.logo_url} folder="site" label="Logo Desa" /><ImageUploadField name="hero_image_url" defaultValue={item?.hero_image_url} folder="site" label="Gambar Utama Beranda" /><div className="field span-2"><label>Tautan Google Maps Kantor</label><input type="url" name="office_maps_url" defaultValue={item?.office_maps_url || ""} /></div><div className="field"><label>Facebook</label><input type="url" name="facebook_url" defaultValue={item?.facebook_url || ""} /></div><div className="field"><label>Instagram</label><input type="url" name="instagram_url" defaultValue={item?.instagram_url || ""} /></div><div className="field"><label>YouTube</label><input type="url" name="youtube_url" defaultValue={item?.youtube_url || ""} /></div><label className="checkbox-field"><input type="checkbox" name="is_published" defaultChecked={item?.is_published} /> Publikasikan profil desa</label><div className="form-actions"><button className="button primary" type="submit">Simpan Profil</button></div></form>; }
