import Link from "next/link";
import { savePopulation } from "@/app/admin/(dashboard)/actions";
import { populationCategories, populationTypeLabels } from "@/lib/config";
import type { Hamlet, PopulationStatistic } from "@/lib/types";
export default function PopulationForm({item,hamlets}:{item?:PopulationStatistic|null;hamlets:Hamlet[]}) { return <form className="admin-panel form-grid" action={savePopulation}>{item?<input type="hidden" name="id" value={item.id}/>:null}<div className="field"><label>Jenis Statistik</label><select name="statistic_type" required defaultValue={item?.statistic_type||"gender"}>{Object.entries(populationTypeLabels).map(([value,label])=><option value={value} key={value}>{label}</option>)}</select><small>Kategori dapat diketik sesuai kebutuhan.</small></div><div className="field"><label>Kategori</label><input name="category" required defaultValue={item?.category||""} list="population-categories"/><datalist id="population-categories">{Object.values(populationCategories).flat().map((category)=><option value={category} key={category}/>)}</datalist></div><div className="field"><label>Wilayah</label><select name="hamlet_id" defaultValue={item?.hamlet_id||""}><option value="">Seluruh Desa</option>{hamlets.map((hamlet)=><option value={hamlet.id} key={hamlet.id}>{hamlet.name}</option>)}</select></div><div className="field"><label>Jumlah</label><input type="number" min="0" name="total" required defaultValue={item?.total??0}/></div><div className="field"><label>Tahun Data</label><input type="number" name="period_year" required defaultValue={item?.period_year||new Date().getFullYear()}/></div><div className="field"><label>Urutan Tampilan</label><input type="number" name="display_order" defaultValue={item?.display_order||0}/></div><label className="checkbox-field"><input type="checkbox" name="is_published" defaultChecked={item?.is_published}/> Publikasikan</label><div className="form-actions"><button className="button primary" type="submit">Simpan</button><Link className="button danger" href="/admin/penduduk">Batal</Link></div></form>; }
