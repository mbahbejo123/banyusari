import{notFound}from"next/navigation";import OfficialForm from"@/components/admin/OfficialForm";import{requireAdmin}from"@/lib/auth";import type{Official}from"@/lib/types";
export default async function OfficialEditorPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const{supabase}=await requireAdmin();let item:Official|null=null;if(id!=="baru"){const{data}=await supabase.from("officials").select("*").eq("id",id).maybeSingle();if(!data)notFound();item=data as Official;}return <><div className="admin-heading"><h1>{item?"Edit Perangkat":"Tambah Perangkat"}</h1></div><OfficialForm item={item}/></>}
