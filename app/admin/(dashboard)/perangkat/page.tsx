import Link from "next/link";import {deleteOfficial} from "../actions";import {requireAdmin} from "@/lib/auth";import type{Official}from"@/lib/types";
export default async function OfficialsAdminPage(){const{supabase}=await requireAdmin();const{data}=await supabase.from("officials").select("*").order("display_order");const items=(data||[])as Official[];return <><div className="admin-heading"><div><h1>Perangkat Desa</h1><p>Kelola nama, jabatan, foto, dan urutan perangkat desa.</p></div><Link className="button primary" href="/admin/perangkat/baru">Tambah Perangkat</Link></div><div className="admin-panel"><table className="admin-table"><thead><tr><th>Foto</th><th>Nama</th><th>Jabatan</th><th>Status</th><th>Aksi</th></tr></thead><tbody>{items.map(item=><tr key={item.id}><td>{item.photo_url?<img className="table-image" src={item.photo_url} alt=""/>:"-"}</td><td>{item.name}</td><td>{item.position}</td><td>{item.is_published?"Tampil":"Draf"}</td><td className="actions-cell"><Link className="button small primary" href={`/admin/perangkat/${item.id}`}>Edit</Link><form action={deleteOfficial.bind(null,item.id)}><button className="button small danger">Hapus</button></form></td></tr>)}</tbody></table></div></>}
