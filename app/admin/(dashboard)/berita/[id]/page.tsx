import{notFound}from"next/navigation";import PostForm from"@/components/admin/PostForm";import{requireAdmin}from"@/lib/auth";import type{Post}from"@/lib/types";
export default async function PostEditorPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const{supabase}=await requireAdmin();let item:Post|null=null;if(id!=="baru"){const{data}=await supabase.from("posts").select("*").eq("id",id).maybeSingle();if(!data)notFound();item=data as Post;}return <><div className="admin-heading"><h1>{item?"Edit Berita":"Tambah Berita"}</h1></div><PostForm item={item}/></>}
