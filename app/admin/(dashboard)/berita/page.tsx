import Link from"next/link";import{deletePost}from"../actions";import{requireAdmin}from"@/lib/auth";import type{Post}from"@/lib/types";
export default async function PostsAdminPage(){const{supabase}=await requireAdmin();const{data}=await supabase.from("posts").select("*").order("created_at",{ascending:false});const items=(data||[])as Post[];return <><div className="admin-heading"><div><h1>Berita dan Pengumuman</h1><p>Simpan sebagai draf atau terbitkan ke website.</p></div><Link className="button primary" href="/admin/berita/baru">Tambah Berita</Link></div><div className="admin-panel"><table className="admin-table"><thead><tr><th>Judul</th><th>Jenis</th><th>Status</th><th>Terbit</th><th>Aksi</th></tr></thead><tbody>{items.map(item=><tr key={item.id}><td>{item.title}</td><td>{item.post_type}</td><td>{item.status}</td><td>{item.published_at?new Date(item.published_at).toLocaleString("id-ID"):"-"}</td><td className="actions-cell"><Link className="button small primary" href={`/admin/berita/${item.id}`}>Edit</Link><form action={deletePost.bind(null,item.id)}><button className="button small danger">Hapus</button></form></td></tr>)}</tbody></table></div></>}
