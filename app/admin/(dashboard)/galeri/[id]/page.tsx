import{notFound}from"next/navigation";import GalleryForm from"@/components/admin/GalleryForm";import{requireAdmin}from"@/lib/auth";import type{GalleryItem}from"@/lib/types";
export default async function GalleryEditorPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const{supabase}=await requireAdmin();let item:GalleryItem|null=null;if(id!=="baru"){const{data}=await supabase.from("gallery_items").select("*").eq("id",id).maybeSingle();if(!data)notFound();item=data as GalleryItem;}return <><div className="admin-heading"><h1>{item?"Edit Galeri":"Tambah Galeri"}</h1></div><GalleryForm item={item}/></>}
