import Link from"next/link";import{deleteGallery}from"../actions";import{requireAdmin}from"@/lib/auth";import type{GalleryItem}from"@/lib/types";
export default async function GalleryAdminPage(){const{supabase}=await requireAdmin();const{data}=await supabase.from("gallery_items").select("*").order("display_order");const items=(data||[])as GalleryItem[];return <><div className="admin-heading"><div><h1>Galeri</h1><p>Kelola dokumentasi foto desa.</p></div><Link className="button primary" href="/admin/galeri/baru">Tambah Foto</Link></div><div className="admin-panel"><table className="admin-table"><thead><tr><th>Foto</th><th>Judul</th><th>Kategori</th><th>Status</th><th>Aksi</th></tr></thead><tbody>{items.map(item=><tr key={item.id}><td><img className="table-image" src={item.image_url} alt=""/></td><td>{item.title}</td><td>{item.category||"-"}</td><td>{item.is_published?"Tampil":"Draf"}</td><td className="actions-cell"><Link className="button small primary" href={`/admin/galeri/${item.id}`}>Edit</Link><form action={deleteGallery.bind(null,item.id)}><button className="button small danger">Hapus</button></form></td></tr>)}</tbody></table></div></>}
